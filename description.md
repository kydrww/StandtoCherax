# StandtoCherax
StandtoCherax – Vehicle Converter is a utility that converts Stand vehicle .txt files into Cherax-compatible .json files. It supports batch conversion by allowing users to select a folder containing vehicle files and automatically generates converted outputs in a Cherax_&lt;FolderName> directory.
